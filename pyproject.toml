[project]
name = "agentic-data-platform"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
# Ingestion is Airbyte-platform-side now, declared in terraform/ — the only
# Python left is the Stripe sandbox seeder and the Cube MCP server.
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=2.0.0",
    "python-dotenv>=1.2.2",
    "stripe>=12.0.0",
]
